# Optional Python executable path. The app does not automatically load .env files.
# Export this in your shell before npm run dev; see README for Windows examples.
# PAA_PYTHON=/absolute/path/to/python3.12

# Tests only: absolute isolated user-data directory; never use real meetings.
# PAA_TEST_DATA_DIR=/absolute/path/to/isolated-test-data

# 复制为根目录 .env；已有 .env 时只补充需要的项目，不要覆盖。
# 桌面公司 Web 地址（公开配置）。npm run dev / build / package 会读取。
# 留空始终使用游客模式；客户端不提供手动填写地址的入口。
# 发行包填写员工可访问的 HTTPS 地址，例如 https://assistant.example.com。
# 修改后重新启动开发进程或重新构建；不读取 .env.company。
PAA_DESKTOP_COMPANY_URL=

# 以下运行时选项仍需在启动前通过 shell 设置，不从 .env 自动导入。
# Optional Python executable path.
# PAA_PYTHON=/absolute/path/to/python3.12

# Tests only: absolute isolated user-data directory; never use real meetings.
# PAA_TEST_DATA_DIR=/absolute/path/to/isolated-test-data

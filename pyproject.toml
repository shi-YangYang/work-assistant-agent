[project]
name = "paa-core"
version = "0.1.0"
description = "Local standard-library core for Personal Agent Assistant"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.paa]
entry-point = "src/python/paa_core/__main__.py"
test-command = "python -m unittest discover -s tests/python -v"

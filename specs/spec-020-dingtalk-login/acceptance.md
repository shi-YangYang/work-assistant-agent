# 钉钉登录验收

## Result

**PASS（软件实现与本地验证）**。部署与鉴权／Web 分别由未参与对应实现的 Agent 独立检查。无未解决的软件阻塞项；真实钉钉授权与公网部署尚未验收。

## 覆盖与证据

- 服务端：公司配置加密、成员准入、员工自动开户与并发复用、绑定冲突、单次 state、浏览器／公司／配置版本校验、密码验证与会话撤销。`test_dingtalk.py`＋`test_company.py` 22 项通过，已有 `test_management.py` 8 项通过；使用固定钉钉响应及独立测试库。
- Web：`dingtalk.test.ts`、`session-drafts.test.ts`、`api.test.ts` 共 26 项通过；Web 类型检查、改动文件 ESLint 和普通 Web 构建通过。后续仅补面包屑文案、弹窗错误位置与按钮 type，按局部范围格式化和复核，未重复全量检查。
- 日常环境：备份数据库和独立主密钥后迁移至 `0009_dingtalk_login`，迁移前后账号、密码哈希、角色及消息／报告／工作数量一致；重启 Web、API、worker。管理员原密码退出后重新登录成功，默认关闭钉钉时入口隐藏。
- 界面：日常配置与账户页可访问，回调地址与密码状态正确；桌面布局及 390 CSS px 下表单宽度正常，无横向溢出，临时视口已恢复。未修改真实钉钉配置或发送测试业务消息。
- 部署：域名／公网 IP／bootstrap Compose 渲染、Caddy 配置与仅 ACME 的 bootstrap、临时证书 TLS 握手及轮换重载通过；Certbot 5.4.0 参数、续期失败传播与重载重试已检查。临时自签证书不代表公开可信证书已签发。

独立审查发现的旧密码并发签发、限流连接池等待、回调 Referrer 覆盖，以及草稿弹窗错误不可见／重试误提交问题均已修复并定向复核。

真实联调后补充权限缺失提示与安全错误定位：服务端钉钉模块 20 项、Web 钉钉模块 5 项通过；增量独立复核通过，日常服务已重启生效。

## 待外部验证

真实配置可获取企业应用凭证，但成员查询返回 `88 / 60011`，明确缺少 `qyapi_get_member` 权限。用户开通并发布后继续验证首次开户、重复登录、已有账号绑定、密码设置和外部／授权范围外成员拒绝；通讯录授权范围须限定为允许登录的员工，并与应用可使用范围一致。

实际签发可信 IP 证书并验证自动续期，从公司网络与实体手机流量验证 HTTPS、钉钉回调及返回。配置步骤见 [使用指南](../../docs/setup.md#钉钉登录配置)。未提交／推送，未运行远端 CI。

# Acceptance 017

## Result

**PASS** · 2026-09-15 · 独立验收 Agent `/root/accept_spec017`。

本次验收对应当前 `dev` 工作区，尚未 commit／push，不代表远端 CI 或生产容量已通过。

## Spec Coverage

- **R1**：固定处理槽、1～8 配置边界、单进程锁和成员运行约束共同限制并发；领取与恢复复用公司／成员／任务锁序、租约与 fence。未发送预约和已开始但结果未知的请求分别处理，关闭及槽启动故障不会被伪装为完成。
- **R2**：定时、手动和重试汇入受控报告流程；模型只输出四栏目，来源、当前授权、报告版本及保存由程序检查。内容／候选与成功凭据同事务提交；编辑、提交、删除和停用后的晚到结果有保护，旧保存凭据可恢复，管理员不能看到候选及私有草稿。
- **R3**：周期安排、员工有效区间及待办均持久化；后续周期生效，既有待办保留时区和截止快照。无工作仍须汇报，提交完成待办；删除与权限撤销停止提醒。团队统计使用当期应汇报记录，分页及过滤由服务端处理。
- **R4**：通知按待办合并为就绪／即将截止／逾期阶段，重复调度不重新置未读；已读与提交分离。当前周期优先，历史补齐与提醒更新均有批量上限，过期历史不补发模型请求。通知仅含定位元数据，读写重新验证身份。
- **Web 与迁移**：员工待办／通知、管理员汇报情况及规则生效说明与共享契约一致；筛选、分页及详情返回有保留路径。0007 为增量迁移，既有资料保留；日常环境备份、升级和双视口检查由协调 Agent 完成。

## Tests

独立阅读 Spec、Plan、实现差异及相关测试，核对 [实施记录](implementation.md) 的验证范围：105 项既有相关服务测试最终通过、17 项新增服务测试通过、2 项 Web 定向测试及相关类型／Lint／格式／构建／Compose 配置检查通过。首轮失败及修正原因保留在实施记录中。

复用协调 Agent 在日常 Web 的 1280×800、390×844 操作证据及数据保留检查。本次未发现需额外复现的具体问题，遵守停止规则，不重复已通过的测试，也未改动业务实现。

## Issues / Required Rework

未发现本次需求范围内的阻塞问题，无必需返工。

## Regression Risks

并发测试使用固定响应及同步屏障，不代表真实供应商吞吐量、限流或长期运行容量。本轮未调用真实模型、执行生产部署、运行 Electron／发行包检查或远端 CI。提醒只提供站内记录；原有日报从迁移后的下一周期开始，不倒推历史欠交。

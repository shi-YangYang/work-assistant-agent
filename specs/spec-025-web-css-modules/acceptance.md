# 验收

## Result

**FAIL：样式迁移范围审查通过，完整验收仍有缺口。**

独立验收 Agent `accept025` 审查源码差异与验证证据，未发现本次引入的明确代码缺陷；未修改业务代码或放宽测试断言。

## 已核对

- Web 组件显式导入就近 CSS Modules，全局仅保留基础样式；两端分别拥有主题、选择器与品牌 CSS，共享包仅保留图片。
- 公共组件通过受限样式接口定制，无跨业务私有 CSS 依赖。滚动恢复、聊天内容观察和菜单定位使用稳定属性，没有认证、接口或业务规则变化。
- Web 类型、Lint、改动文件格式、Module 引用解析与普通构建通过；Desktop 普通构建通过，基础声明与迁移前一致。
- 浏览器完成 172 组页面／交互前后对照、9 项边界、4 项滚动与菜单行为、5 项最终生产预览检查。对照中保留了临时数据、焦点和细微渲染差异的说明，不将所有图片声称为完全一致。

## 未通过与未验证

1. **Web 全量测试为 180 通过、3 失败**，在迁移前源码副本上已复现同样失败：
   - `architecture.test.ts`：`progress-edit.ts` 对状态请求模块的既有依赖违反架构约束。
   - `component-composition.test.ts`：旧测试无法找到已封装组件内的指定输入框。
   - `list-feedback.test.ts`：相同序号的聊天反馈断言与现有实现不符。
2. Desktop 日常窗口的品牌、浅色主题与列表已查看；原生 UI 自动化返回 `noWindowsAvailable`，**深色切换及选择控件交互未验证**。构建通过不能替代这两项实测。

当前没有确认的 CSS 代码返工项。三个既有失败应另行处理或由用户明确接受；桌面补两项定向交互检查，不扩大为后端、模型、完整打包或远端 CI。

证据保留在本地忽略目录 `artifacts/spec025/`：`verification.md`、前后截图、`web-tests.log`、`baseline-test-failures.log` 及对应检查日志。临时测试账号、样本与专用凭据已清理；未提交或推送。

# 验收

## Result

**FAIL：样式迁移范围审查通过，Desktop 两项交互仍未验证。**

独立验收 Agent `accept025` 审查源码差异与验证证据，未发现本次引入的明确代码缺陷；未修改业务代码或放宽测试断言。

## 已核对

- Web 组件显式导入就近 CSS Modules，全局仅保留基础样式；两端分别拥有主题、选择器与品牌 CSS，共享包仅保留图片。
- 公共组件通过受限样式接口定制，无跨业务私有 CSS 依赖。滚动恢复、聊天内容观察和菜单定位使用稳定属性，没有认证、接口或业务规则变化。
- 样式迁移的类型、Lint、改动文件格式、Module 引用解析与普通构建通过；Desktop 普通构建通过，基础声明与迁移前一致。
- 浏览器完成 172 组页面／交互前后对照、9 项边界、4 项滚动与菜单行为、5 项最终生产预览检查。对照中保留了临时数据、焦点和细微渲染差异的说明，不将所有图片声称为完全一致。

## 测试失败修复

独立验收时 Web 测试为 180 通过、3 失败，三项均在迁移前源码复现。用户随后授权协调 Agent 直接修复：接口错误解析移入工作 API 层；表单测试识别 `FormField` 的公共接口；聊天去重保留同版本下的操作卡片和任务状态更新。

修复后 Web 全量 **185／185 通过**，类型及改动文件 Lint 通过，源码已按项目配置格式化；未放宽原有断言。新增回归覆盖重复快照、卡片变更／清除、过期快照与终态更新。结果由协调 Agent 验证，不冒充新的独立验收；日志为 `artifacts/spec025/web-fixes-{tests,typecheck,lint}.log`。

## 剩余未验证

Desktop 日常窗口的品牌、浅色主题与列表已查看；原生 UI 自动化返回 `noWindowsAvailable`，**深色切换及选择控件交互未验证**。构建通过不能替代这两项实测；只需补定向交互检查，不扩大为后端、模型、完整打包或远端 CI。

证据保留在本地忽略目录 `artifacts/spec025/`：`verification.md`、前后截图及检查日志。临时测试账号、样本与专用凭据已清理。

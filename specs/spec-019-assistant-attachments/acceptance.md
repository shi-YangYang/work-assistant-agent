# Acceptance — 工作助手发送与附件体验

## Result

**PASS** · 2026-09-16 · 返工后的独立验收 `accept019_final`。首次验收发现的图片画廊键盘缺陷已关闭；结合已完成的相关验证，当前范围未发现阻断问题。未验证的设备、平台和真实模型结果仍单列，不扩大本结论。

## Spec Coverage

- R1／R2：选择、粘贴与拖拽共用校验；混合附件、录音追加、原键重试和会话／身份隔离已有实现及相关用例。真实剪贴板和未发送混合草稿经协调 Agent 实操。
- R3：真实 MP3、HEIC、XLSX 样本覆盖解码、可见单元格／公式保存值、隐藏范围、错误与资源上限。私有 UUID 原件通过文件流解析，无 schema 变更。
- R4：发送前后图片／PDF 入口、同源私有字节、局部渲染和资源释放已核对。普通与严格部署 CSP 下 PDF 实际渲染证据有效；返工后画廊左右键覆盖初始关闭按钮焦点，保留边界、转换锁定、Esc 与焦点恢复。
- R5／权限：方向、透明背景、细字与有界长图样本明确区分原件和派生输入；预览重新鉴权、最终发布与删除同步、注销拒绝和孤立清理有直接用例。未宣称真实模型准确率提升。

## Tests

复核 Spec／Plan、实施报告、既有验收、相关测试断言及最终 `Modal`／`ImageGallery` 实现。采用已完成的 36 个唯一服务端、41 个唯一 Web 定向用例及类型、改动文件 Lint／格式和普通 Web 构建结果，不无理由重跑。Linux ARM64 原生依赖解码及正常用户环境 GUI／严格 CSP 证据见 [实施摘要](implementation.md#验证)。

本轮独立检查确认：可选且有类型约束的 `onKeyDown` 仅由图片画廊传给当前 `<dialog>`，没有全局键盘监听；方向键仍调用受边界与转换状态约束的 `choose`，其他按键不被阻止，既有取消及关闭恢复焦点逻辑未改。其他对话框未传入此处理器，发送前后入口使用同一修复后的画廊。

采用返工后两文件格式化、一次 Web 类型检查与 diff 检查结果，以及协调 Agent 的日常 GUI 实操：两图打开后焦点在关闭按钮，立即 Right 到第 2 张、Left 回第 1 张、边界保持；Esc 关闭并回到原缩略图。验收者未再次运行测试、修改业务代码、触发 CI 或付费模型。

## Issues / Required Rework

独立验收结束时无待返工项。随后真实联调发现“MP3 未提供”的来源误判，协调 Agent 已按用户要求直接补齐附件与转写来源，定向回归及最终真实混合附件复测通过。样本、过程与结果边界统一见 [真实模型联调](implementation.md#真实模型联调--2026-09-16)；该追加验证不冒称新的独立验收，也不扩大为所有模型／平台均已通过。

历史：首次独立验收 `accept019_attachments` 为 **FAIL**，发现 P2：刚打开画廊时关闭按钮的焦点在 `.media-preview` 左右键监听范围之外，协调 Agent 已实际复现。新实施 Agent `rework019_gallery` 将处理器提升到当前对话框；本轮独立源码复核与上述定向 GUI 结果支持关闭该问题。

## Regression Risks / 未验证范围

本次独立验收未执行真实模型、实体 iPhone Safari／Android Chrome、Windows 服务及 Linux x64 部署；窄屏与固定模型响应不能替代这些结果。其后协调 Agent 已补真实模型小样本与发送后预览，结果及限制见上述联调记录。文件实际拖放、真实触摸仍未在日常界面实操。Linux ARM64 定向安装／解析和本地严格 CSP 验证不等于完整生产部署验收。

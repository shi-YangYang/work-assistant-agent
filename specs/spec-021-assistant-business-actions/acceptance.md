# Acceptance

**PASS** — 已独立复核业务写入、权限、确认与恢复，以及最终答复返工；未发现阻断项。

## 覆盖与证据

- 页面与 Agent 共用工作／报告业务服务；标题新建、截止日期、字段补丁、同名澄清、普通材料仅建议、角色／公司隔离均有相关检查。报告继续异步使用专用流程，提交及单条删除绑定实际版本和影响预览。
- 操作回执与业务事务一起保存；重试、换工具调用 ID、部分成功、确认冲突和来源失效不会重复执行或绕过权限。删除只留下最小结果，不保留被删除正文。
- 旧完成句正则已移除。独立语义请求区分已有查询事实与本轮执行叙述，程序核对完整分段、证据 ID 和查询工具类型；执行结果统一取持久回执。核对失败仍保留成功业务结果，未核实正文不提前流出。
- 实施报告的 77 项相关服务检查、2 项 Web 检查、类型与 Lint 通过。返工后 64 项业务／权限／反馈检查及收紧证据类型后的 69 项相关业务／文档／语音／恢复检查通过；范围有重叠，不累计为独立测试总数。本次验收未无理由重跑。
- 真实配置 `deepseek-v4.1-flash` 联调已覆盖手动和聊天创建、相对日期、工作更新、报告生成／编辑／确认提交、服务重启恢复确认卡、取消删除、讨论不写入及汇报待办查询。原始结果在 `artifacts/spec021-real/`。
- 最新 `verified-read-and-update.json` 与 `verified-admin-followup.json` 同时保留正确查询事实和真实操作结果；`verified-persisted.json` 证实报告仍为已提交第 3 版、指定工作仅更新所需字段、管理员创建本人的关联督办且员工原工作第 2 版不变，分别用 5／6 次调用在原预算内完成。
- 协调 Agent 实际检查桌面的创建、结果卡、确认／取消与详情返回，以及 390×844 手机视口的完整报告预览和确认；最新会话刷新后正确展示已提交事实与工作更新结果。

## 限制

自然语言意图和答复分类仍受模型语义质量影响；权限、版本、确认和幂等由服务端强制约束。手机结果为浏览器视口验证，未使用实体手机。未执行 Electron、发行包或远端 CI；未 commit／push。

# Acceptance — Spec 007

## Result

PASS · 2026-09-12。新的独立验收 Agent `/root/spec007_reacceptance` 根据 Spec、Plan、首次 FAIL 和最新返工记录，定向复核三项 R1 修复及其直接回归风险；未修改业务代码。三项工程缺陷已关闭，未改动的 R2～R4 沿用首次独立审查与已通过的定向证据。日期半输入清除的返工后 GUI 复查因 macOS 锁屏未执行；本结论不代表该实机检查或 Windows／远端 CI 已通过。

## Spec Coverage

- **R1**：完整历史、字面匹配、自然日边界、稳定排序与转写／纪要定位已有实现及定向测试；协调 Agent 在日常环境确认搜索命中进入实际内容、打开不自动播放、手动定位回听、返回保留查询。返工后的分页失效、完整命中预览与日期原生控件重置均符合要求，关闭依据见下方历史记录。
- **R2**：专用持久化操作校验名称，不改动 ID、音频与 AI 纪要标题；持久化及非法名称已有测试。实际 UI 空名称错误与取消操作已验证，未改名用户真实会议。
- **R3**：一致 SQLite 副本、全文生成、16 KiB 分块、快照取消／过期清理、目标旁临时文件替换及剪贴板失败处理符合方案。协调 Agent 已确认复制成功及原生保存 Markdown 文件的标题、正文和时间信息；全文、多字节、失败保留目标等复用模块测试证据。
- **R4**：schema 5 删除意图先于文件清理持久化；录音／转写／纪要忙碌复核与新任务、晚到写入共享 Repository 锁。恢复先于业务任务初始化，失败保留重试入口，清理受控路径及关联行；主进程等待媒体流真实关闭。已读取对应故障、外键、迁移回滚、符号链接及媒体测试，未删除用户真实会议。

## Tests

已通过的命令及各轮定向结果统一见 [实施报告](implementation.md#验证) 与其 R1 定向返工段落。本次复验读取相关代码和新增测试，复用最新的搜索 Vitest 2 通过／5 跳过、长词 Python 1 通过（180／200 字两个子用例）、renderer 类型及三个改动 TS／TSX 文件的格式／Lint 通过结果，没有重复测试或执行构建。首次验收的无用户数据 `preview` 探针曾复现长词缺陷。

协调 Agent 在项目 `npm run dev` 默认环境提供的非破坏性 GUI 证据覆盖搜索定位／回听、日期筛选、复制／原生保存、非法名称提示及取消改名与删除；900 × 640 窗口的浅深主题中搜索、日期和操作入口可达，深色菜单定位及 Esc 关闭弹窗通过，完成后恢复用户的系统主题。四条既有会议保持完整。

## Issues 与验收历史

首次独立验收 `/root/spec007_acceptance` 为 FAIL，原因与本次关闭依据如下：

1. **P2 查询切换混合分页 — 已关闭。** 原实现可在 250 ms 防抖期间用新条件加旧结果长度请求分页，并使新首屏失效。现在 `QueryGeneration` 保存最后成功应用的查询；输入、清除和 IME 组词立即失效旧代次，首屏应用前及分页进行中拒绝新分页，晚到响应不能写入。新定向测试覆盖防抖窗口、重复分页、旧分页、IME 和清除。
2. **P2 长关键词预览截断命中 — 已关闭。** 首次探针的 180 字关键词只留下 135 字，无法高亮。现在右边界至少为实际命中末尾加 45 字，保留完整命中且正文不超过 290 字；查询路径的 180／200 字测试确认完整命中及有界上下文。
3. **P3 日期半输入清除残留 — 工程修复已关闭。** 原实机可在清除后残留 `0011/月/日`。现在原生 `onInput` 记录编辑状态，使只有半输入时也显示“清除”；清除同时清空筛选、重置编辑状态并递增两端 `key`，重新创建日期 DOM 控件，消除旧控件的原生缓冲。代码路径与类型／格式检查已确认，返工后实机复查尚未执行。

## Regression Risks

本轮未发现三项修复的具体未解决缺陷或直接回归。日期半输入清除的原生 GUI 行为仍留有实机验证限制：协调 Agent 遇到 macOS 锁屏，已请用户解锁，不能宣称已复查。Windows 实机、发行包、真实麦克风／ASR、付费模型及远端 CI 本轮未执行。删除和导出故障验证使用受控临时资料；用户会议未发生改名、删除或覆盖。

## Required Rework

无必需工程返工。锁屏解除后的日期半输入 GUI 复查仅补该场景；未修改的 R2～R4 与已通过检查不重跑。

# Acceptance — Spec 010

## 用户审查后宽度返工（主 Agent 自查）

2026-09-13，用户要求主 Agent 独立修复内容区域靠左或窄列居中造成的大块留白。取消内容区固定限宽后，日常浏览器 1440×900 的账户、用途、工作详情、聊天白色区域均为 1176px，距右侧工作区两边各 24px；1920×1080 的模型服务区域为 1656px；390×844 的账户区域仍为 358px、两边各 16px。上述页面及内容容器均未横溢，截图和尺寸在 `artifacts/spec010/width-rework/`。本次仅 CSS 与文档增量，使用 Prettier 并检查 diff，未重跑测试、类型、构建或 CI；未新增独立 Agent 验收。下方 PASS 和 64 张截图记录对应此次用户反馈之前的实现。

## Result

**PASS** · 2026-09-13 · 全新独立最终复验 Agent `spec010_final_acceptance`。

基于 `9912de4` 之后的 Web 实现与两项定向返工，独立阅读 Spec、Plan、实施报告、实际 diff 和行为测试，查看最终截图及尺寸记录。两轮阻塞均已关闭，未发现与本次范围直接相关的未解决问题。本轮只维护本报告，未修改业务代码、操作浏览器／用户资料或重新运行已通过的工程检查。

## Spec Coverage

| 范围 | 最终依据 |
| --- | --- |
| L01～L02 列表与工具栏 | 工作摘要与下一步收敛，状态和更多操作靠近标题；完整正文留在详情。列表、成组日期、报告类型与操作区采用统一尺寸和间距。前后截图覆盖团队三个标签、工作、报告和成员。 |
| L03 响应式 | 最终 `narrow-team` 的 `.page` 为 `clientWidth=320 / scrollWidth=320`，`mobile-team` 为 `390 / 390`；独立看图确认姓名、状态、箭头稳定排列，日期完整。760px 使用手机导航，761／1023px 为 72px 图标侧栏，1024px 为 216px 文字侧栏，断点记录无 document 或 page 横溢。 |
| L04～L05 设置 | 独立查看双列模型连接、1440×900 三项用途与保存同屏、账户底部、手机星期 4＋3、员工只读周期摘要。环境来源保留显式导入，未改变接口或配置。登录、外观沿用既有结构，随共享样式完成前后覆盖。 |
| L06 正文 | 独立查看原始上报和深色聊天；Markdown 仅构造 React 文本节点，HTML 转义、图片不嵌入、链接仅接受明确的 http／https／mailto，长文本可换行，原始上报和已保存内容不改写。 |
| L07 来源导航 | 独立核对本地返回路径校验、直接入口 fallback、嵌套来源 state 和查询参数传递；原始上报名称正确且只保留壳层返回。主 Agent 在日常浏览器完成团队员工报告 `tab=reports&kind=daily` → 报告 → 工作 → 来源 → 逐层返回，查询保留。 |
| L08 编辑 | 独立核对 AutoTextarea 的受控值、按宽度 ResizeObserver 调整和 cleanup，不用断点切换组件身份；前后报告编辑截图显示短文本不再占四个大框，保存／稍后继续可达。主 Agent 实际检查未保存报告字段在 390→768→390 后保留，随后还原原文并清除本轮前端草稿，未保存业务。 |
| L09 弹层与反馈 | 短屏成员弹窗、查找菜单可滚到末项，标题／关闭和末尾操作可达；320px 账户保存入口完整。未保存服务改为“丢弃草稿”，已保存服务仍明确撤销影响与失败反馈。Esc 由 React 关闭回调决定，busy 拒绝关闭时不会被原生默认动作隐藏。 |

视觉资料由主 Agent 在日常 Web 采集：[前后对照](../../artifacts/spec010/audit.html)、[基线清单](../../artifacts/spec010/before/manifest.json)、[最终清单](../../artifacts/spec010/after/manifest.json)、[断点尺寸](../../artifacts/spec010/after/breakpoints.json)。52 个基线模板均有对应新截图，最终共 64 张；清单中的文件齐全，已记录的 document 和容器尺寸无横溢。本轮独立抽看最终 320／390 团队、短屏登录／成员弹窗／查找菜单、窄账户底部、模型连接／用途、员工和管理员规则、工作列表、来源正文、深色聊天／用途，并比较报告编辑前后；没有声称逐张独立重审 64 张。

浅深色延续共享 Electron 语义主题，未改共享主题文件。深色代表图实际视口为 903×807；其名称中的 `desktop` 表示桌面浏览器，不代表 1440px 实测。结束时主 Agent 已恢复管理员、浅色、默认视口和日常 `/team`，清除本轮服务草稿；没有保存服务或发起模型调用。

## Tests

复用 [实施报告](implementation.md) 中与最终代码对应的通过结果：Web typecheck、编辑文件定向 ESLint、导航 2 项／Markdown 3 项行为测试、Web build；返工后 `ui.tsx` 单文件 ESLint 通过，CSS 按 S0 完成项目 Prettier 与 diff 检查。最终复验未无理由重跑这些检查。

本次改动限定 Web 展示、导航及必要测试；无新增依赖、API、数据库、服务端、Electron 或 CI 修改。未运行 CI、全项目测试、录音／ASR 或发行包验证。

## 已关闭的验收历史

- **首轮 FAIL · `spec010_acceptance`：** 原生 dialog Esc 默认关闭绕过服务删除 busy 保护，失败信息可能留在隐藏弹窗。新实施 Agent 在 `Modal.onCancel` 中先 `preventDefault()` 再调用原有 `onClose`；第二轮及本轮独立代码复核确认关闭权回到 React 状态。普通草稿弹窗 Esc 关闭、重新打开及丢弃由主 Agent 实操确认；未删除真实服务来制造失败。
- **第二轮 FAIL · `spec010_reacceptance`：** 新增 320px 证据曾显示团队行箭头掉行，`.page` 宽 320px、内容宽 333px；原因是时间行全宽叠加左边距和姓名最小宽度。新实施 Agent 仅将手机团队行改为四列 Grid、姓名可收缩、时间独占第二行网格区域；本轮以更新后的 320／390 图和尺寸确认修复。当前同名截图已是修复后证据，333px 为历史测量记录。

## Regression Risks / 未验证项

实体手机、Windows、Safari 原生日期、真实软键盘、首次强制改密专用样本、真实删除失败及所有网络／冲突状态没有实测；视口截图与代码核对不替代这些条件。普通账户改密布局已覆盖，但未实际更改密码。Markdown 限基础展示，不承诺完整 CommonMark／表格功能。

**Required Rework：无。**

# Acceptance — Spec 011

## Result

**PASS · 2026-09-13，返工后独立验收。** 验收者 `/root/accept_spec011_final` 未参与首轮实施或返工；独立读取规则、Spec、Plan、实施报告、测试和当前 diff，确认首轮两项 P1 已修复，未发现本次范围内尚未解决的阻断问题。本轮只更新本记录，不修改业务代码，不操作日常数据库、浏览器或进程。

## Spec Coverage

| 范围 | 结论与证据 |
| --- | --- |
| R1 面包屑 | 按授权对象获取名称，来源链限定业务层级并拒绝循环／外部路径；刷新／直达可构造本人或团队父级。导航定向断言及协调 Agent 的五级路径、刷新、实际父级返回与 390px 完整路径检查通过 |
| R2 管理与删除 | 员工曾提交报告保护、公司／所有者授权、修订冲突、共享业务快照保留、原文件清理失败重试和已删周期不再生成有定向证据。候选来源变更递增版本，旧确认被拒绝；三处已展开分页重新校验全部可见范围，详见返工复核 |
| R3／R4 角色与成员 | 导航、品牌、命令入口及 `/reports` 路由共同排除管理员个人报告；服务端拒绝管理员生成／编辑／提交，worker 仅调度员工报告。管理员助手与本人工作继续可用；员工管理的列表／新增／停用／重置范围在服务端一致，其他管理员私有材料仍不可读；CLI 初始化管理员未被 API 限制误伤 |
| R5 时间与时区 | 中文标签与 IANA 值分离，非预设时区保留，夏令时地区不显示全年固定偏移。四个时间字段复用整框与图标可打开的分钟选择器，保留输入、清空和格式校验；关闭按钮明确为 `type="button"`。协调 Agent 已检查中文菜单锚定与四个文本区打开，取消后公司 revision 未变 |
| R6 输入与限制 | 固定高度样式只作用于聊天文字区；协调 Agent 检查桌面 128px、390px／短屏 96px，长文内部滚动、无紫色焦点、无横溢。超过 5 MiB 图片仅触发限制弹窗，关闭后文字草稿保留；原服务端数量／格式／大小限制未放宽，录音上限停止与保存路径保留 |
| R7 会话 | API 检查会话所有者／公司／删除状态和 replyTo 归属；聊天历史限定当前会话，已确认工作来源可按权限跨会话检索。草稿及异步回写绑定原会话，录音离开有明确处理。删除保留业务来源、拒绝发送与确认旧草稿，checkpoint 使用同一所有者锁防止晚到恢复。员工 API 定向检查与管理员日常新建／重命名／搜索／移动列表／草稿切换证据相互补充 |

## 首轮 FAIL 与返工复核

首轮验收者 `/root/accept_spec011` 于同日给出 **FAIL**，两项问题均直接违反 R2；新的实施 Agent `/root/rework_spec011` 完成修复后，由本轮新验收者复核。

1. **P1：候选来源变化未使删除确认失效，已修复。** 原路径为管理员确认来源 A 后，在途任务把 B 写入 candidate，但报告版本不变，旧确认可以连带删除 B。[harness.py](../../src/python/paa_server/agent/harness.py) 现在对正文或 candidate 的成功写入统一递增 `revision`、更新修改时间；与 [deletion.py](../../src/python/paa_server/deletion.py) 的所有者锁及版本检查配合。UI 使用影响接口返回的版本提交删除。新增 `test_candidate_sources_invalidate_open_delete_confirmation` 实际调用候选写入工具，断言影响从 1 条变 2 条、版本递增、旧 DELETE 返回 409，A／B 消息及 B 的附件内容和文件仍保留。

2. **P1：已展开旧页长期保留已删除正文，已修复。** 原助手、团队原始上报和个人报告列表将永久 `older` 缓存拼接到仅刷新的第一页。[paged-resource.ts](../../src/web/paged-resource.ts) 改为只保存已展开的最早边界，每次从第一页沿新游标读取并整体替换结果；新消息挤页时继续读到该边界，删除旧行或旧游标不保留其正文。已确认 `Assistant.tsx`、`Settings.tsx` 的团队原始上报、`Records.tsx` 的个人报告均实际使用该资源。轮询、窗口重新可见／获得焦点及本窗口管理事件触发重新校验，403／404 清空全部已加载内容，资源切换取消旧读取。6 项分页测试覆盖这三类路径、删除旧行／游标、新消息挤页、403／404 和晚到结果隔离。

## Tests

本次权限、迁移与删除属 S3，Web 交互属 S2；复用 [实施摘要](implementation.md) 中与当前代码对应的通过证据，不机械重跑：

- 返工后专用测试库 management＋recovery **12 项通过**；Web 分页资源 **6 项通过**；实际改动 TS 文件经 Prettier 整理，相关 ESLint 与 `typecheck:web` 通过。
- 未受返工影响的首轮证据：company／management／recovery 17 项、transcript recovery 7 项；Web navigation／api／audio-capture／progress-edit 共 12 项，相关静态检查及普通 Web build 通过。management／recovery 的最终有效结果以上一条返工后的 12 项为准，未重复 build。
- 协调 Agent 已备份并迁移日常库：16 张原业务表原字段摘要一致，5 条原消息归入 2 个默认会话，附件未变。迁移证据为本地 `artifacts/spec011/migration-result.json`；页面检查摘要见实施记录，本地截图含 `mobile-breadcrumbs.png`、`mobile-conversation.png`、`timezone-menu.png`。

本轮独立验收核对了上述测试断言与业务接线，没有发现需要新增执行的具体疑点，因此未再次运行已通过的检查。日常删除确认只查看后取消；破坏性与竞态测试使用专用测试库，原有用户资料未被删除。

## Issues

无待返工问题。文档状态与最终交付由协调 Agent 依据本结论收尾。

## Regression Risks / 未验证边界

- 员工账户的日常 UI 登录切换因浏览器自动化连接／交互异常未完成，不能表述为员工整套界面实测通过；员工权限采用固定输入服务端检查及角色路由代码复核，共用会话与输入组件采用管理员日常交互证据。未发现由该工具限制推导出的具体代码缺陷。
- 日常汇报规则未保存改动；中文值／恢复路径、分钟选择与校验按实现复核，实际检查覆盖菜单与四个输入区打开和取消。真实麦克风、180 秒录音效果、实体手机和其他浏览器未在本轮实测。
- 没有 Electron、依赖或 CI 配置改动。未运行真实付费模型、ASR、发行包或部署验证；尚未 commit／push，未触发或核对远端 CI，不宣称这些项目通过。

## Required Rework

无。

# Acceptance — Spec 006

## Result

**PASS · 2026-09-11，第二轮独立工程复验。** 验收 Agent：`/root/spec006_reacceptance`，独立于全部实施者及首轮验收者。首轮确定的列表轮询容量缺陷已关闭，没有发现其他具体工程阻塞，可提交进行本次 CI 验证。此为提交前工程结论：本次 macOS／Windows CI 尚未运行，不能宣称全部交付检查通过；收到对应提交的结果后补记。

## Spec Coverage

| 范围 | 本轮判断 |
| --- | --- |
| R1：语义主题、字号、对比度、减少动效 | 代码覆盖浅／深／系统主题及持久化、状态文字、焦点样式与 reduced-motion。协调 Agent 已实测主题重载并恢复 system；实际色值浅深 18 组达到目标，最低文字 4.73:1、控件 3.64:1，不能视为穷举全部状态和像素。 |
| R2：导航、会议页签、设置分组、阅读位置 | 结构与已确认 Spec 一致；内存导航不改变 URL。设置编辑器按服务保留，隐藏内容不参与焦点。协调 Agent 已验证非秘密名称草稿跨页／跨编辑器保留，未保存并重载清除。列表状态容量缺陷已由共享队列与可见行订阅修复。 |
| R3：录音条、引用、单播放器、命令与菜单 | 录音订阅位于 App，页签共用唯一播放器；引用沿 cursor 分页找到目标后聚焦。协调 Agent 已验证命令过滤导航、同会议切页签播放时间连续、原文时间定位、离开会议清理播放器，以及 900 × 640 下播放器和倍速菜单可达。窄窗引用展开改为面板可滚动、正文至少 160 px；第 61 段引用与新增录音竞态仍待本次 smoke 运行证据。 |
| 既有业务与工程边界 | 多服务、动态模型、预设、连接测试、流式默认值、下载与原文接口仍接入原有业务。未修改 Python、共享 IPC、安全、数据目录、依赖或构建系统。四个变更 smoke 文件已逐段核对页面／页签和定位器，未发现另一个确定的隐藏控件点击阻塞；本轮未运行这些 smoke。 |

## Tests

- 复用[实施报告](implementation.md)中的 web／node TypeScript、定向 ESLint、已安装 Prettier 和 diff 检查结果；返工单测 `tests/desktop/meeting-processing.test.ts` 已通过，第二轮独立审查确认其使用真实 `JsonLineClient` 测量 100 行及 20 次旧请求未返回的重入，并检查最大 pending 为 4、取消后的晚到更新与后续查询、计时清理及 ASR → 纪要状态转换。未重复执行已通过且未再修改的检查。
- 第二轮独立核对共享队列及 App／可见行集成：额度只在实际请求结束的 `finally` 释放；删除订阅不提前释放额度；旧 Entry 身份校验阻止更新新行或继续查询纪要。单一 timer、活动 5 秒／静态 30 秒刷新、离屏／隐藏／断连取消和明确刷新入口与返工目标一致。
- 独立核对新增窄窗 smoke：断言真实 `clientHeight ≥ 160`、滚动后正文与面板交叠至少 100 px，并对三个引用按钮逐一执行 trial 点击；后续实际播放与第 61 段定位同时检查播放器实例。断言具有行为意义，但本轮尚未运行，不记为 PASS。
- 日常环境 CUA 由协调 Agent 在项目 `npm run dev`、默认用户目录完成，以上只记录其实际提供的证据；验收者没有代称自己操作过桌面。已有 4 条历史和已下载模型得到保留。
- 本次提交的 macOS／Windows CI **尚未运行**。旧基线的成功不能替代本次结果。物理 Windows 设备、真实付费服务以及本次新增真实录音交互均未在本轮验证。

## Issues

### 已关闭：首轮 P2 — 历史行独立轮询占满核心请求容量

首轮 `/root/spec006_acceptance` 判定 **FAIL**：每行独立轮询全部历史，单行 `alive` 清理不能限制全局并发。首轮独立内存复现使用实际组件及 `JsonLineClient` 挂载 100 行、暂缓核心响应，得到 `pending=64` 和 36 行 busy，影响共用通信容量的录音／打开会议操作。

返工新增 `meeting-processing-queue.ts`，全局最多 4 条 IPC，可见行才订阅，取消旧请求仍保留其占用额度直至结束。上述回归与第二轮独立代码审查共同关闭此问题；没有调整协议或放宽 64 条通信上限。当前无未解决的确定缺陷。

## Regression Risks

- 窄窗引用正文的最小高度与外层滚动已实现；实际布局及第 61 段引用行为仍需本次 CI 验证，不以 CSS 阅读代替运行。
- 远端 smoke 须验证录音导航与快速结束、第 61 段引用定位、菜单边缘、服务草稿、安装版重连及退出；双平台 CI 成功也不等于物理 Windows 麦克风验证。

## Required Rework

无待实施返工。协调 Agent 继续提交并核对该提交的两平台必需 CI，保留实际通过、跳过和未执行的边界；若本次检查失败，按具体失败处理后再复验。

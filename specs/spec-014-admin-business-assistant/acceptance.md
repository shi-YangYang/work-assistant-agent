# Acceptance — 管理员业务问答与督办事项

## Result

**PASS — 2026-09-14，独立验收与真实补验完成。** 工程、电脑／窄屏界面、真实同名澄清，以及实际模型建议→编辑→确认→本人工作持久化均有证据。两条授权标签丢失路径与“只有文字承诺、没有建议”三项 P1 已关闭。

## Spec Coverage

| 范围 | 验收依据 |
| --- | --- |
| R1／R2 业务查询与来源 | 授权先于工具读取和模型输入；已确认工作、发布报告与关联材料可追溯，私人内容被排除。覆盖日期／历史版本、分页、同名、真实员工拒绝及团队接口 403；来源弹窗和当前详情返回已有界面验证。 |
| R3 本人督办 | 确认、编辑和关联变更继承依赖，复核权限与版本；幂等、忽略、源删除后本人工作保留有回归。实际模型建议经独立 API 编辑确认，员工原记录不变。 |
| R4 权限撤销 | 工具、模型输入、发布、历史、摘要和 checkpoint 复核权限。原标签丢失复现及新增编辑／来源／派生报告回归通过。 |
| R5／兼容 | 电脑与 390px 界面操作、相关 Web 检查通过；旧员工主流程通过，日常库备份后增量迁移。 |

## Tests and Evidence

工程命令、次数和返工实现集中见 [Implementation](implementation.md)，新的独立验收者复核代码与已有证据，不重复已通过检查。下列原始结果保留在本机、由 Git 忽略的 `artifacts/spec014/`，不提交密钥或数据库备份。

| 证据文件（相对该目录） | 实际结果与边界 |
| --- | --- |
| `fixed-ui-fixture.json`、`gui-confirmation-audit.json` | 固定输入经真实业务工具生成建议，独立 CUA 在 390×844 编辑／确认，刷新仍可见且无横向截断；仅新增 1 个管理员工作，员工原 2 项内容／版本不变。此项未调用模型。 |
| `real-recovery.json`、`real-flow.json`、`real-followup-after-fix.json` | 前轮真实查询收尾修正后从 checkpoint 发布，模型调用 6 → 6；员工拒绝通过；督办先缺少实际建议，修复后遇到 500。失败记录保留。 |
| `retest/diagnostic*.json`、`retest/request-payload-provenance.json` | 最小无工具请求 200；实际框架工具请求流式 500、非流式 200。请求来自 `process_job → create_deep_agent`，捕获检查 1 项通过、捕获过程外部调用 0；不能据此认定非流式是通用修复。 |
| `retest/results-nonstream.json` | 基元律动 `glm-5.3-flash` 的合成公司切为非流式后，同名请求识别两位张晨并先澄清，未查询员工工作或生成建议。 |
| `retest/followup-checkpoint.json`、`retest/results-resumed.json` | 同一非流式督办在 3 次成功模型调用后再次 500；本人工作、员工匹配和采购查询已完成。仅一次原输入／原配置 checkpoint 续跑后 `succeeded`，生成 1 个真实 pending 建议、1 个业务来源，确认前本人工作为 0。辅助脚本终态断言曾误报，随后只核对保存结果，没有为此重发模型请求。 |
| `retest/confirmation.json` | 独立验收者对真实模型建议 PATCH 编辑下一步、POST 确认，再获取列表／详情并核对 DB；本人工作 `e85171ec-c348-4bc0-b060-dfc2b036008c` 归属、授权、业务关联、修订和本人原消息来源正确，员工原 2 项内容／版本未变，未用固定输入替代。 |

新增真实结果的编辑／确认通过 HTTP API 完成，因为当轮 iab 控制面不可用；界面覆盖沿用之前独立 CUA 的证据，不能混称为同一次完整 GUI 联调。真实模型请求由协调 Agent 执行，独立验收者负责结果与确认核对。

合成公司、账号、业务、任务、checkpoint 及加密配置已按精确 ID 清理；日常 Web／Electron 资料、模型配置和浏览器账号未改。迁移前日常库备份保留。2026-09-15 按用户要求清理过期联调脚本、缓存及重复请求材料，保留上述原始结果和正式回归测试。

## Failure / Rework History

首轮独立验收 **FAIL**：两条编辑路径丢失团队授权，降权后仍返回团队派生内容；真实督办另有 `drafts=[]`。新的实施 Agent 修复授权继承及建议补正，由新的独立验收者复核；原权限复现已归入 `tests/server/test_business_assistant.py`。

工程完成一度被过早描述为整体 PASS；发现真实同名及督办验证缺口后更正为 FAIL。本轮真实澄清、建议与独立编辑确认补齐后才记最终 PASS，未用模型接口 500 代替功能通过证据。

## Regression Risks / Required Rework

无当前阻断项。流式与非流式均出现过 HTTP 500，内部或上游具体根因仍未确定；一次 checkpoint 恢复成功不代表服务长期稳定。真实同名答复包含已授权员工短 ID，虽未越权，仍有不符合提示词的展示偏差。长期容量、生产部署及所有平台未在本轮验证；未触发 CI。

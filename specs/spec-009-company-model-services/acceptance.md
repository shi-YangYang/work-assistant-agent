# Acceptance — 公司级模型服务管理

2026-09-12 · 首轮独立验收后，由新的独立验收 Agent 复核定向返工；范围为 `7441f62` 之后当前 dev 工作区的 Spec 009 改动。

## Result

**PASS**（工程验收）。首轮发现的管理员检测撤销缺陷已关闭；本轮独立复核限于该修复及直接影响，其余范围复用首轮审查与已有验证证据。此结论对应独立复验当时的实现；后续用户直接修改的增量由主 Agent 自行验证，见文末边界。

## Spec Coverage

- R1／R2：服务端管理员与公司范围、CSRF、只写密钥、换址重新输入、版本冲突、模型目录／手填和非敏感草稿已有实现及定向证据。
- R3／R4：空默认参数、预设保护、三类用途、流式完整工具组装、预算入口，以及 Job 固定配置／新配置尝试与输入修订隔离已核对。业务调用与管理员检测均在出站前核对原服务及凭证可用性；并发编辑不切换本次检测配置。
- R5：固定图文／工具／语音检测、有限调用次数、公司额度／并发、错误脱敏及无业务写入已有证据。检测中撤销服务后，当前未发出项失败，后续项保持未测试。
- R6：复用协调 Agent 的正常 Web 实测：桌面浅色、360px 深浅色、长 ID、预设、敏感草稿离开保护、保存清空 Key 和用途操作。代码核对了草稿白名单与会话失效后的组件释放。
- 迁移／凭证：增量 schema、AES-GCM 认证数据、主密钥配对与缺失／替换失败、显式环境导入／无回退及部署挂载已核对；开发库备份迁移见 `artifacts/spec009/development-migration.json`。

## Tests

复用 [实施报告](implementation.md) 中已经通过的 15 项新增服务端用例、16 项相关恢复用例、2 项 Web 草稿用例及类型／格式／lint／普通 Web 构建证据，没有重复执行。

首轮独立风险复现：`node scripts/company.mjs test artifacts/spec009/acceptance_probe_revocation.py --tb=short -s`。仅使用独立测试库和固定 HTTP 响应，**1 项失败，1.29 秒**：第一个文字请求期间删除已保存服务成功，随后仍发出图片及两次工具请求，总请求数为 4，结果全部标记通过。

返工定向命令及完整覆盖范围见 [实施报告的返工记录](implementation.md#独立验收后的定向返工)：**8 项通过，2.80 秒**。本轮独立阅读实际查询、调用顺序和回归断言，确认覆盖文字后／图片后／工具首次返回后撤销、草稿新 Key、并发保存保留原修订、两种 ASR 出站前撤销，以及正常草稿和公司并发限制。未发现需要追加验证的具体未解风险，未重复运行已通过命令。

## Issues / Required Rework

首轮 **P1 已关闭**：`model_services.py` 的 `probe.request_key` 在每次聊天／ASR 出站前，通过现有请求事务查询同公司、未撤销服务及 `expectedRevision` 的凭证并校验解密。标量查询避免 ORM 缓存，不新增连接或行锁；原配置及本次草稿保持不变。撤销错误保留明确的 `revoked` 状态，并结束后续请求。原始 FAIL 事实与复现保留在上述记录，当前无必需返工。

协调 Agent 已重启正常 Web／API／worker 加载返工代码；本轮复验未修改业务代码、开发库、浏览器或进程。

## Regression Risks / 未验证边界

后续用户直接授权的基元律动文字上报、补充关联与周报真实链路已由主 Agent 跑通；期间发现并修复的引用、检索与事务时序问题，以及 5 项定向回归见 [增量记录](implementation.md#用户直接修复与真实联调2026-09-12)。按用户要求没有再次派独立验收 Agent，不能称为新一轮独立验收。

真实供应商图文／ASR、长期实际业务报告质量、生产部署／恢复／容量及实体手机仍未验证；单个合成业务场景不代表普遍准确率。验收阶段未运行 CI、桌面录音或安装包检查；提交与推送状态以 Git 记录为准。

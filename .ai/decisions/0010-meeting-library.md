# 本地会议管理

- 使用有界、分页的本地关键词查询，不引入检索服务；参数化子串匹配并转义通配符，支持一两个汉字。英文大小写遵循 SQLite 的 ASCII 规则。
- 不强制 FTS5 trigram，避免短词限制；不提供语义搜索或复杂查询语法，数据规模形成瓶颈后再评估索引。
- 用户会议名称与 AI 纪要标题分开保存；改名不重生成纪要，不改变 ID 和引用。
- 导出由 main 调用系统保存框，核心提供完整快照，不把当前分页当成全部内容。
- 删除需确认，并协调任务、文件与数据库；永久删除无应用内撤回，测试不能删除用户既有会议。

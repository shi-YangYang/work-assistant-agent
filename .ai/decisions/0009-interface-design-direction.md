# 界面设计方向

- 各端使用中性黑白灰、轻量表面和清晰的消息／工具／确认层级；具体色值只在设计规范维护，翻书登录交互保留。
- 共享视觉语言，各端独立维护 CSS，不引入重复 UI 库。
- 导航、设置子页、会议页签各司其职；已结束会议默认纪要，录音中默认文字。
- 录音跨页连续，播放器保持唯一，服务编辑保留草稿；纪要、原文和音频联动，命令面板复用已有操作。
- 视觉调整不改变权限、IPC 信任边界、接口和数据位置。

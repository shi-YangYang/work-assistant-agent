# 项目骨架

初始化保留 MIT LICENSE，建立 `constitution/`、`specs/`、`.ai/`、`tests/`、`docs/`；技术未确定时只建目录，不提前生成业务脚手架。

业务曾统一放在 `src/`，现由 [多端目录方案](0015-multi-client-repository.md) 接续。固定 Agent 目录仍保留，空目录有实际内容后可移除 `.gitkeep`。

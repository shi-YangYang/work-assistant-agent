# 安装包自带运行环境

- macOS／Windows 安装包包含 Python 核心和依赖，用户无需安装解释器或手动启动服务。
- 正式包只启动自身资源中的核心，不回退系统 Python 或源码目录；缺失时明确报错。
- 开发使用 `.venv` 或 `PAA_PYTHON`，普通用户界面不提供解释器配置。
- 转写模型由用户在应用内下载。分发验证资源路径、进程退出，以及无预装 Python／Node 的启动环境。
